volpind/repo/hw/00/02-ssh-agent

> First I ran  → eval $(ssh-agent) to see the PID of the ssh-agent

What good is the PID to you?

> then using ssh-add I added private key passphrase to ssh-agent.
>
> More specifically the ssh-agent outputs the environment variable that I needed to connect,
> as “eval” loads the variables to the terminal environment.

That's right!

+10 points (maximum)

Total = 10+0 points

If anything seems wrong, let me know (mail). Thanks :).
                                      - David
